<%= form_with(model: notifier) do |form| %>
  <% if notifier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(notifier.errors.count, "error") %> prohibited this notifier from being saved:</h2>

      <ul>
        <% notifier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :notifier_type, style: "display: block" %>
    <%= form.collection_select :notifier_type, Notifier::NOTIFIERS.keys.sort, :to_s, :to_s, {include_blank: false} %>
  </div>

  <div>
    <%= form.label :webhook_url, style: "display: block" %>
    <%= form.text_field :webhook_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
